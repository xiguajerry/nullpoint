package me.nullpoint.mod.modules.settings;

public enum Placement {
   Vanilla,
   Strict,
   Legit,
   AirPlace;

   // $FF: synthetic method
   private static Placement[] $values() {
      return new Placement[]{Vanilla, Strict, Legit, AirPlace};
   }
}
